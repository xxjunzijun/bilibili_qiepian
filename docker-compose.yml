services:
  qiepian:
    build: .
    container_name: qiepian
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
      - ./recordings:/app/recordings
